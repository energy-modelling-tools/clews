# clews.github.io

Jekyll website for **CLEWs** (Climate, Land (Food), Energy and Water systems approach), intended to be published under the **Energy Modelling Tools** GitHub Pages site.

- **Theme**: `energy-modelling-tools/page-template` (via `remote_theme`)
- **Expected URL**: `/clews/` (set via `baseurl` in `_config.yml`)

